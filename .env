# Network selection (mainnet or sepolia)
NETWORK=${NETWORK:-mainnet}

# Client selection (geth or reth)
CLIENT=${CLIENT:-geth}

# Base data directory
HOST_DATA_DIR=./${CLIENT}-data

# Source the appropriate client configuration
if [ "$CLIENT" = "geth" ]; then
    source .env.geth
else
    source .env.reth
fi